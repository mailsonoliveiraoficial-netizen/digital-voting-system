name: flutter_application_1
description: "Sistema de Votação Rainha do Rodeio"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ^3.1.0

dependencies:
  flutter:
    sdk: flutter
  # Bibliotecas do Firebase
  firebase_core: ^2.24.2
  cloud_firestore: ^4.14.0
  # Validadores e Máscaras
  cpf_cnpj_validator: 2.0.0
  mask_text_input_formatter: ^2.8.0
  # Ícones
  cupertino_icons: ^1.0.8
  share_plus: ^12.0.1
  firebase_app_check: ^0.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true